<!doctype html>
<html lang="en">

    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Manager View</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
            integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    </head>
    <body>
        <div class="report-menu-sales-screen">
            <th>
                <div class="container">
                    <h2>Sales Report</h2>
                    <p>The total income of each item within the given timeframe of</p>
                    <table class="table table-bordered border border-5"> 
                        <tr class="border border-5">
                            <th class="border border-5 bg bg-info-subtle">Item Sold</th>
                            <th class="border border-5 bg bg-info-subtle">Total Income</th>
                        </tr>
                        <% if (result && result.length > 0) { %>
                            <% result.forEach(row => { %>
                                <tr class="border border-5">
                                    <th class="border border-5"><%= row.order_item %></th>
                                    <th class="border border-5"><%=row.item_total%></th>
                                </tr>
                            <% }); %>
                        <% } else { %>
                            <p>No results found.</p>
                        <% } %>

                       
                        <%if (result && result.length > 0) { %>
                            <%let maxItemTotal = Number.MIN_VALUE; %>
                            <% let minItemTotal = Number.MAX_VALUE; %>
                        

                            <% result.forEach(row => { %>
                                <% maxItemTotal = Math.max(maxItemTotal, row.item_total); %>
                            <% }); %>

                            <% result.forEach(row => { %>
                                <% minItemTotal = Math.min(minItemTotal, row.item_total); %>
                            <% }); %>
                        
                            <p>Maximum Item Total: <%= maxItemTotal %></p>
                            <p>Minimum Item Total: <%= minItemTotal %></p> 

                        <% } else {%>
                                <p>No results found.</p>
                        <% } %>
                        

                </table>
                </div>
            </th>
        </div>

    </body>
   
    <script src="report_sales.js"></script>

</html>