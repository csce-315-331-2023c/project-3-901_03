<!doctype html>
<html lang="en">

    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Manager View</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
            integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    </head>
    <body>
        <div class="report-menu-restock-screen">
            <th>
                <div class="container">
                    <h2>Total Usage</h2>
                    <p>Displays the total amount of how many ingredients were used within the given timeframe</p>
                    <table class="table table-bordered border border-5"> 
                        <tr class="border border-5">
                            <th class="border border-5 bg bg-info-subtle">Ingredient</th>
                            <th class="border border-5 bg bg-info-subtle">Total Usage</th>
                        </tr>
                        <% if (result && result.length > 0) { %>
                            <% result.forEach(row => { %>
                                <tr class="border border-5">
                                    <th class="border border-5"><%=row.ingred_name%></th>
                                    <th class="border border-5"><%=row.total_usage%></th>
                                </tr>
                            <% }); %>
                        <% } else { %>
                            <p>No results found.</p>
                        <% } %>


                        <%if (result && result.length > 0) { %>
                            <%let maxItemTotal = Number.MIN_VALUE; %>
                            <% let minItemTotal = Number.MAX_VALUE; %>
                        

                            <% result.forEach(row => { %>
                                <% maxItemTotal = Math.max(maxItemTotal, row.total_usage); %>
                            <% }); %>

                            <% result.forEach(row => { %>
                                <% minItemTotal = Math.min(minItemTotal, row.total_usage); %>
                            <% }); %>
                        
                            <p>Maximum Item Total: <%= maxItemTotal %></p>
                            <p>Minimum Item Total: <%= minItemTotal %></p> 

                        <% } else {%>
                                <p>No results found.</p>
                        <% } %>
                        
                </table>
                </div>
            </th>
            
        </div>

    </body>
   
    <script src="report_restock.js"></script>

</html>